# Environment Configuration for Real Estate AI Graphic Designer

# Google Cloud Project (Required for Imagen)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Alternative env var names (supported)
# GCP_PROJECT_ID=your-project-id

# Gemini API Key (Required for text generation and evaluation)
GEMINI_API_KEY=your-gemini-api-key

# GCS Bucket for training datasets (Optional - for LoRA training)
GCS_TRAINING_BUCKET=your-training-bucket

# Server Configuration
HOST=0.0.0.0
PORT=8002

# Service Account (Alternative to GOOGLE_APPLICATION_CREDENTIALS)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
